import { CapacitorConfig } from "@capacitor/cli";

export default {
  appId: "xyz.spellsword",
  appName: "Spellsword",
  webDir: ".output/public",
  server: {
    androidScheme: "https",
  },
} as CapacitorConfig;
